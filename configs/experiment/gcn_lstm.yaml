# Layer 2: GCN + LSTM (static graph, no adaptive, no context)

_base_: "../default.yaml"

model:
  name: gcn_lstm
  use_graph: true
  use_adaptive_graph: false
  use_context: false

training:
  batch_size: 32
  max_epochs: 75
